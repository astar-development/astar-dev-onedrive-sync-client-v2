# misc notes

## possible logging updates

a version with JSON logs

a version with per‑feature meters

a version that integrates with your TreeView loading overlay

a version that logs UI thread exceptions and unhandled Avalonia errors - done

A metrics‑enhanced version (e.g., count UI exceptions)

A crash‑reporting integration (Sentry, etc.)

A logging overlay inside Avalonia for debug builds

a draggable version

a collapsible version

a version with log level filtering

a version that shows metrics (OpenTelemetry counters)

a version that shows UI thread FPS and layout timings

Add a Clear Logs button

Add log level filtering

Add a search box

Add a performance tab (FPS, layout time, render time)

add log level filtering

add a collapsible header

add a metrics panel (OpenTelemetry counters live in UI)